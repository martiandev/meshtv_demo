<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="facilityVM"
            type="com.ph.bittelasia.meshtvlibrary.viewmodel.iptv.MeshFacilityViewModel"/>
        <variable
            name="category"
            type="com.ph.bittelasia.meshtvlibrary.database.entity.iptv.MeshFacilityCategory"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:onClick="@{() -> facilityVM.getCatResult(category.id)}"
        android:padding="16dp"
        android:layout_margin="2dp"
        android:background="@drawable/item_background"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <TextView
            android:id="@+id/tv_name"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:textStyle="bold"
            android:textSize="24sp"
            android:textColor="@color/h_inn_green"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="@{category.category_name}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
