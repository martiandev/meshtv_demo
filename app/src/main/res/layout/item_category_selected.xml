<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="channelVM"
            type="com.ph.bittelasia.meshtvlibrary.viewmodel.iptv.MeshChannelViewModel" />
        <variable
            name="channelCategory"
            type="com.ph.bittelasia.meshtvlibrary.database.entity.iptv.MeshChannelCategory" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:onClick="@{() -> channelVM.getCatResult(channelCategory.id)}"
        android:padding="16dp"
        android:layout_margin="2dp"
        android:background="@color/h_inn_green"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_name"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:textStyle="bold"
            android:textSize="24sp"
            android:textColor="@color/white"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="@{channelCategory.category_name}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>


    </androidx.constraintlayout.widget.ConstraintLayout>





</layout>
