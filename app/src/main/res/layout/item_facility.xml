<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <import type="com.ph.bittelasia.meshtvlibrary.preference.object.iptv.MeshSTB"/>
        <variable
            name="stb"
            type="MeshSTB"/>
        <variable
            name="facility"
            type="com.ph.bittelasia.meshtvlibrary.database.entity.iptv.MeshFacility"/>
        <variable
            name="listener"
            type="com.ph.bittelasia.meshtvlibrary.util.imageloader.ImageLoaderListener" />

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/cl_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <ImageView
            android:id="@+id/iv_icon"
            app:imageUrl="@{stb.host+':'+stb.port+facility.img_uri}"
            app:listener="@{listener}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:focusable="true"
            android:clickable="true"
            android:layout_width="100dp"
            android:layout_height="100dp"/>

        <TextView
            android:id="@+id/tv_name"
            android:text="@{String.valueOf(facility.item_name)}"
            android:textColor="@color/h_inn_green"
            android:textSize="20sp"
            android:layout_margin="8dp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="@+id/iv_icon"
            app:layout_constraintLeft_toRightOf="@+id/iv_icon"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/tv_description"
            android:textColor="@color/h_inn_green"
            android:text="@{String.valueOf(facility.item_description)}"
            android:layout_marginTop="12dp"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="8dp"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@+id/tv_name"
            app:layout_constraintLeft_toRightOf="@+id/iv_icon"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
